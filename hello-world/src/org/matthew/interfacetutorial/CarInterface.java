package org.matthew.interfacetutorial;

public interface CarInterface {
    //abstract methods
    // when create a class from a interface, has to have the following methods
    void start();
    void move(int speed);
}
